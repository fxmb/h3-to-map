export type Hex = {
    hex: string;
    hexCenter: Array<number>;
    hexPolygon: Array<number>;
    hexResolution: number;
    hexAddress?: string;
    hexColor: string;
    hexZoom: number;
    hexColorCode: string;
};